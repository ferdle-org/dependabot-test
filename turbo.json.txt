{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "@zendesk/embeddable-framework#build": {
      "dependsOn": ["@zendesk/web-widget-messenger#build", "@zendesk/web-widget-classic#build"],
      "outputs": ["dist/**"]
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": [],
      "inputs": ["src/**"]
    },
    "lint": {
      "outputs": []
    },
    "dev": {
      "dependsOn": ["dashboard#build"]
    },
    "previewers:build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    }
  },
  "globalDependencies": ["$EMBEDDABLE_FRAMEWORK_ENV"]
}
